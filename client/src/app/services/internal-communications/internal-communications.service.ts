import { Injectable } from '@angular/core';
import { Subject } from 'rxjs';

@Injectable({
  providedIn: 'root',
})
export class InternalCommunicationsService {
  //public loginRequired: Subject<()=>void | null> = new Subject();

  constructor() {}
}
